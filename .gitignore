*.crt
*.key

# Local value overrides that may contain secrets (e.g. --values values.local.yaml)
*.local.yaml
secrets.yaml

# Freebox API token cached by scripts/freebox-dns.py
.fbx_token.json
scripts/.fbx_token.json
__pycache__/
*.pyc
